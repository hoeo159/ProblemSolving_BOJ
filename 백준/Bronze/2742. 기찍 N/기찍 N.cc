#include <iostream>
#include <stdio.h>
#include <algorithm>

using namespace std;

int main()
{
    int i,n;
    scanf("%d", &n);
    for(i=n; i>0; i--){
        printf("%d \n", i);
    }
}
